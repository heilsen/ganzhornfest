CREATE TABLE IF NOT EXISTS busLine (id INTEGER PRIMARY KEY NOT NULL, busLine TEXT NOT NULL, stops TEXT, destination TEXT NOT NULL);

insert:
INSERT INTO busLine(id, busLine, stops, destination) VALUES (?, ?, ?, ?);

deleteAll:
DELETE FROM busLine;
