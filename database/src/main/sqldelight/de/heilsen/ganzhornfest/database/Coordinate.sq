CREATE TABLE coordinate (id INTEGER PRIMARY KEY NOT NULL, lat REAL NOT NULL, lng REAL NOT NULL);

updateLatLng:
UPDATE coordinate
SET lat = :lat, lng = :lng
WHERE id = :id;

insert:
INSERT INTO coordinate(id, lat, lng) VALUES (?, ?, ?);

deleteAll:
DELETE FROM coordinate;
